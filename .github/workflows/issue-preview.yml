name: Issue Preview

on:
  issues:
    types: [opened, reopened]

permissions:
  issues: write

jobs:
  technique-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: new technique') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate JSON preview comment
      run: |
        python3 admin/issue_parsers/parse_technique_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  weakness-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: new weakness') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate JSON preview comment
      run: |
        python3 admin/issue_parsers/parse_weakness_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  mitigation-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: new mitigation') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate JSON preview comment
      run: |
        python3 admin/issue_parsers/parse_mitigation_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  technique-update-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: update technique') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate update preview comment
      run: |
        python3 admin/issue_parsers/parse_update_technique_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  weakness-update-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: update weakness') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate update preview comment
      run: |
        python3 admin/issue_parsers/parse_update_weakness_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  reference-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: new reference') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate reference preview comment
      run: |
        python3 admin/issue_parsers/parse_reference_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  mitigation-update-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: update mitigation') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate update preview comment
      run: |
        python3 admin/issue_parsers/parse_update_mitigation_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  dfcite-relevance-update-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: update dfcite relevance') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate DFCite relevance preview comment
      run: |
        python3 admin/issue_parsers/parse_update_dfcite_relevance_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  move-technique-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'content: move technique') && contains(github.event.issue.labels.*.name, 'form input') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate move preview comment
      run: |
        python3 admin/issue_parsers/parse_move_technique_issue.py \
          --issue-body-file issue_body.md \
          --output comment.md

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

  trwm-submission-preview:
    if: "${{ contains(github.event.issue.labels.*.name, 'trwm') }}"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install pydantic pybtex

    - name: Write issue body to file
      run: printenv ISSUE_BODY > issue_body.md
      env:
        ISSUE_BODY: ${{ github.event.issue.body }}

    - name: Generate TRWM preview comment
      run: |
        python3 admin/issue_parsers/parse_trwm_submission.py \
          --project-root . \
          --issue-body-file issue_body.md \
          --output comment.md \
          --labels-output labels.txt

    - name: Post comment on issue
      run: |
        gh issue comment "$ISSUE_NUMBER" --body-file comment.md
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}

    - name: Apply auto-detected labels
      run: |
        if [ -s labels.txt ]; then
          while IFS= read -r label; do
            gh issue edit "$ISSUE_NUMBER" --add-label "$label"
          done < labels.txt
        fi
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ISSUE_NUMBER: ${{ github.event.issue.number }}
